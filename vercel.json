{
    "builds": [{
        "src": "payment_failure/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.12" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "payment_failure/wsgi.py"
        }
    ]
}